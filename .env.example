# Copy to .env and fill in. Never commit .env.

# Anthropic API - powers the signal parser (text + chart vision)
ANTHROPIC_API_KEY=

# Optional: webhook URL for fill/exit/error notifications
NOTIFICATION_WEBHOOK_URL=
